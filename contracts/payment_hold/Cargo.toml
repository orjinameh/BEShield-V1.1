[package]
name = "beshield-escrow"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "20.0.0" # Use the latest stable Soroban SDK

[lib]
crate-type = ["cdylib"]

[profile.release]
overflow-checks = true